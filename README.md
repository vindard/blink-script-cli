## Description

This is a helper for fetching transactions list from Blink API for all wallets. To use:

- Create `.env` file
  ```env
  # .env

  export TOKEN="<token-here>"

  ```

- Make `run` executable with `$ chmod u+x run`

- Run with `$ ./run`
